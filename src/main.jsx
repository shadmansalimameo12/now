// Main app entry point
import ReactDOM from 'react-dom/client';
import App from './App.jsx';
import './index.css';

// Root element e app render korsi
ReactDOM.createRoot(document.getElementById('root')).render(
  <App />
);